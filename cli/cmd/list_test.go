package cmd_test

import "testing"

func Test_list(t *testing.T) {
	cases := []struct {
		name    string
		wantErr bool
	}{
		{},
	}

	for _, tc := range cases {
		t.Run(tc.name, func(t *testing.T) {
		})
	}
}
